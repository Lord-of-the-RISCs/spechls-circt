[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "spechls-xdsl"
version = "0.1.0"
description = "xDSL representation of the SpecHLS dialect"
requires-python = ">=3.10"
dependencies = ["xdsl"]

[tool.pytest.ini_options]
testpaths = ["tests"]
